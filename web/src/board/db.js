// Board + blogs dataset. Emptied — no demo data. Jobs, capabilities, metrics
// and posts populate from the evidence pipeline; until then every section
// renders its empty state.
export const DB = { jobs: [], caps: [], mets: [], posts: [] }
