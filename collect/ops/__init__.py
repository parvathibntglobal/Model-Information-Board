"""E9 ops. The five alerts, coverage data, and the nightly chain.

The first thing in this lane that runs when nobody is watching, which is why
`chain.py` is about visibility before it is about work: a stage that did not run
must be distinguishable from a stage that ran and found nothing.
"""
